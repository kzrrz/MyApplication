<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/barraSupColor</item> <!--Color de la barra superior -->
        <item name="colorPrimaryDark">@color/colorNegro</item> <!-- Color de la barra de notificaciones -->
        <item name="colorAccent">@color/pimarioOscuro</item> <!-- -->
        <item name="android:windowBackground">@color/fondo</item>

        <!-- Originales
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item> -->
    </style>

</resources>
